import Editor from '../Editor';
import TextDocument from '../doc/TextDocument';
export interface RenderWhat {
    old?: TextDocument;
    doc?: TextDocument;
}
export declare function rendering(editor: Editor): {
    render: (what?: RenderWhat | undefined) => void;
    destroy(): void;
};
