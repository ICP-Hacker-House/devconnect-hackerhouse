import Editor from '../Editor';
import TextDocument from '../doc/TextDocument';
import { EditorRange } from '../doc/EditorRange';
export interface VirtualRenderWhat {
    old?: TextDocument;
    doc?: TextDocument;
    selection: EditorRange | null;
}
export declare function virtualRendering(editor: Editor): {
    render: (what?: VirtualRenderWhat | undefined) => void;
    init(): void;
    destroy(): void;
};
